/** Velvet sanctuary surface — matches evening/night gradient and orb shader fade. */
export const BREATHE_SURFACE = '#1A0F2E';

/** Soft top lift so the room feels open, not pitch-black. */
export const BREATHE_SURFACE_GRADIENT = ['#2D1B4A', '#1A0F2E'] as const;

/** Canvas fill — tuned to shader midnight edge for seamless orb blend. */
export const BREATHE_ORB_FILL = '#1A0F2E';
